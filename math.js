const lado = 5;
const perimetro = lado * 4;
const area = lado ** 2;

console.log({
    lado,
    perimetro,
    area,
});

const baseTriangulo = 3;
const alturaTriangulo = 4;

const perimetroTriangulo = baseTriangulo + alturaTriangulo ;
const areaTriangulo = (baseTriangulo * alturaTriangulo) / 2;